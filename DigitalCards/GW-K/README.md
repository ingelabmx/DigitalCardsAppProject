# Google Wallet credentials

Do not store Google Wallet service account JSON files in this repository.

For the legacy Web Forms app, set `GOOGLE_APPLICATION_CREDENTIALS` to the full
path of a service account JSON file stored outside source control.
